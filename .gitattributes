# Normalize all text files to LF in the repo and on checkout (all platforms),
# so `dotnet format` (which enforces end_of_line = lf via .editorconfig) is
# deterministic and the CI formatting gate cannot be tripped by CRLF checkouts
# on Windows.
* text=auto eol=lf
